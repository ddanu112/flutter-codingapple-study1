import 'package:flutter/material.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return  MaterialApp(
      home: Center(
        child: Container( width: 50, height: 50, color: Colors.blue )
      )
    );
  }
}

13~14줄 네모박스 생성은 Container, SizedBox 이고 소괄호 안에 width, height 지정해주고 50==1.2cm정도 단위 LP
child 는 자식 피라미터이고
Center을 통해 중심쪽으로 지정
보통 Center나 Align이런 걸 줌
